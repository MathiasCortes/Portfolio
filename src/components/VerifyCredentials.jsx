import React, { useState } from 'react'

export const VerifyCredentials = (user, pass, email) => {
    const [isLogged, setIsLogged] = useState(false)
  return (
      <>
      if (user ==  ) {
        
      }
      </>
  )
}
